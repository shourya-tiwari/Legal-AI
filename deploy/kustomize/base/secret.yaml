# Placeholder ONLY -- real values come from Vault/KMS/sealed-secrets at
# deploy time (docs/v2/ARCHITECTURE.md "Secrets management"), never
# committed here. Kept in the base so the Deployment's secretRef always
# resolves to *something* even before a real secrets pipeline is wired to
# a target; every value below is an empty placeholder, not a working default.
apiVersion: v1
kind: Secret
metadata:
  name: legalai-backend-secrets
type: Opaque
stringData:
  DATABASE_URL: ""
  REDIS_URL: ""
  MEMGRAPH_URI: ""
  MEMGRAPH_USER: ""
  MEMGRAPH_PASSWORD: ""
  POSTGRES_PASSWORD: ""
  GOOGLE_API_KEY: ""
  LLM_BASE_URL: ""
  EMBEDDING_BASE_URL: ""
  RERANKER_BASE_URL: ""
